<Window x:Class="DataBindingYListBox_5.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Estudiantes por Curso" Height="450" Width="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <ListBox Name="lbEstudiantes"
                 Grid.Column="0"
                 SelectionChanged="lbEstudiantes_SelectionChanged">
            <ListBox.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock FontWeight="Bold"
                                       Foreground="DarkBlue"
                                       FontSize="16"
                                       Text="{Binding Name}" />
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </ListBox.GroupStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <TextBlock Text="{Binding Nombre}" Width="120"/>
                        <TextBlock Text="{Binding Edad}" Width="60"/>
                        <TextBlock Text="{Binding Nota}" Width="60"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <TextBlock Name="tbDetalles"
                   Grid.Column="1"
                   FontSize="16"
                   TextWrapping="Wrap"
                   Margin="20"/>
    </Grid>
</Window>
